# Dependências
node_modules/

# Web gerada (Capacitor empacota daqui)
/www/
/dist/

# Capacitor — a pasta android/ É versionada (customizações nativas),
# mas os artefatos de build e coisas de máquina, não:
/ios/
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/captures/
android/.idea/
android/app/release/
.gradle/

# Firebase — config do app Android (baixar do console quando precisar)
android/app/google-services.json
.firebase/
*-debug.log
firebase-debug.log
firestore-debug.log

# Ambiente / segredos — NUNCA versionar
.env
.env.*
!.env.example

# Chaves de assinatura Android — NUNCA versionar
*.keystore
*.jks
keystore.properties
key.properties

# SO / editores
.DS_Store
Thumbs.db
.idea/
*.swp
